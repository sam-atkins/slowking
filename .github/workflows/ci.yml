name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Set up Python 3.11
      uses: actions/setup-python@v4
      with:
        python-version: "3.11"
    - name: Run image
      uses: abatilo/actions-poetry@v2
      with:
        poetry-version: 1.5.1
    - name: Install Taskfile
      uses: arduino/setup-task@v1
      with:
        version: 3.x
    - name: Install dependencies
      run: poetry install
    - name: Lint and format check
      run: task fmt-check
    - name: Test coverage with pytest
      run: task cov
    - name: Run mypy
      run: task mypy


# FIXME built env in build is not shared to lint, test jobs
# https://docs.docker.com/build/ci/github-actions/share-image-jobs/
# or
# https://github.com/cdalvaro/docker-salt-master/blob/c556e06e5527cc2dc95966039858671f5f16cebe/.github/workflows/build-and-test.yml

    # lint:
  #   runs-on: ubuntu-latest
  #   needs: build
  #   steps:
  #   - name: Lint and formatting check
  #     run: task fmt-check
  # test:
  #   runs-on: ubuntu-latest
  #   needs: build
  #   steps:
  #   - name: Test with pytest
  #     run: task test
